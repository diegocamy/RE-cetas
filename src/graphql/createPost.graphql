mutation CreatePost(
  $title: String!
  $content: String!
  $picture: String!
  $time: String!
) {
  createPost(
    data: { title: $title, content: $content, picture: $picture, time: $time }
  ) {
    slug
  }
}
